import { Controller } from '@nestjs/common';

@Controller('blockchain')
export class BlockchainController {
  // All legacy endpoints removed. BlockchainService is now used internally by other modules for NFT and token rewards.
}
